#pragma once

#include <wx/wx.h>

namespace GUI
{
     struct Color
     {
	     wxColor Grullo = wxColor(155, 153, 135);
	     wxColor Gray = wxColor(44, 66, 63);
	     wxColor Pink = wxColor(239, 169, 174);
     } COLOR;
}